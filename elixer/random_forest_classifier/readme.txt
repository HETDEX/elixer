requires sklearn v1.5.2: $ pip install --user scikit-learn==1.5.2


from Erin:

FYI, the training is done in: /work/05350/ecooper/stampede2/cosmos/calibration/hypothesis-testing-cosmos20251006.ipynb

The features are all part of the raw detection output so you can apply them. It's very quick to run so no harm in
 including it but I don't think it's great for low S/N sources. I did find it worked fairly well for S/N>5.5.